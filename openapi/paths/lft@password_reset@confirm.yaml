post:
  tags:
    - OAUTH
  description: An Api View which provides a method to reset a password based on
    a unique token
  operationId: CreatePasswordToken
  parameters: []
  requestBody:
    content:
      application/json:
        schema:
          $ref: '../components/schemas/body/body_48.yaml'
      application/x-www-form-urlencoded:
        schema:
          $ref: '../components/schemas/body/body_49.yaml'
      multipart/form-data:
        schema:
          $ref: '../components/schemas/body/body_50.yaml'
  responses:
    "200":
      content:
        application/json:
          schema:
            $ref: '../components/schemas/body/body_50.yaml'
