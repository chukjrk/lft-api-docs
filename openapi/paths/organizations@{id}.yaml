get:
  tags:
    - PaidOrg
  description: API endpoint that returns all public programs
  operationId: RetrievePaidOrganization
  parameters:
  - name: id
    in: path
    description: A unique integer value identifying this paid organization.
    required: true
    style: simple
    explode: false
    schema:
      type: string
  responses:
    "200":
      content:
        application/json:
          schema:
            $ref: '../components/schemas/inline_responses/inline_response_200_3.yaml'
