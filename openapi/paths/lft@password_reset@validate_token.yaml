post:
  tags:
    - OAUTH
  description: An Api View which provides a method to verify that a token is valid
  operationId: ValidateToken
  parameters: []
  requestBody:
    content:
      application/json:
        schema:
          $ref: '../components/schemas/body/body_45.yaml'
      application/x-www-form-urlencoded:
        schema:
          $ref: '../components/schemas/body/body_46.yaml'
      multipart/form-data:
        schema:
          $ref: '../components/schemas/body/body_47.yaml'
  responses:
    "200":
      content:
        application/json:
          schema:
            $ref: '../components/schemas/body/body_47.yaml'
